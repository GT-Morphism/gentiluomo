import type { CreateTooltipProps } from "@melt-ui/svelte";

export const CONFIG_TOOLTIP_WITH_DELAY: CreateTooltipProps = {
	openDelay: 500,
	closeDelay: 0,
	disableHoverableContent: true,
	forceVisible: true,
};
